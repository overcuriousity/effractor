[package]
name = "effractor-components"
description = "The bundled component library: its catalog, and the rules that turn an architecture into attack steps. No I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
effractor-core = { path = "../effractor-core" }
effractor-format = { path = "../effractor-format" }
serde_json = { version = "1.0.151", features = ["preserve_order"] }

[lints]
workspace = true

[dev-dependencies]
indexmap = "2"
