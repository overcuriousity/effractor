[package]
name = "effractor-solver"
description = "Pure analyses: BDD, cut sets, Monte Carlo, importance, loss exceedance."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
effractor-components = { path = "../effractor-components" }
effractor-core = { path = "../effractor-core" }
effractor-mal = { path = "../effractor-mal" }
libm = "0.2.16"
rand_chacha = "0.10.0"
rand_core = "0.10.1"
serde = { version = "1.0.229", features = ["derive"] }

[lints]
workspace = true

[dev-dependencies]
effractor-format = { path = "../effractor-format" }
proptest = { version = "1.11.0", default-features = false, features = ["std"] }
serde_json = "1.0.151"
